<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <link rel="stylesheet" href="/stylesheets/admin.css">
</head>
<body>
<header>
    <h1>Edit Movie</h1>
</header>
<main>
    <form action="/admin/movie/edit/<%= movie._id %>" method="post">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= movie.title %>" required>

        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= movie.description %></textarea>

        <label for="release_date">Release Date:</label>
        <input type="date" id="release_date" name="release_date"
               value="<%= movie.release_date.toISOString().split('T')[0] %>" required>

        <label for="duration">Duration (minutes):</label>
        <input type="number" id="duration" name="duration" value="<%= movie.duration %>" required>

        <label for="genre">Genre:</label>
        <input type="text" id="genre" name="genre" value="<%= movie.genre %>" required>

        <label for="imageUrl">Image URL:</label>
        <input type="text" id="imageUrl" name="imageUrl" value="<%= movie.imageUrl %>" required>

        <button type="submit">Save Changes</button>
    </form>
    <a href="/admin/screening/view/<%= movie._id %>">View Screenings</a>
    <a href="/admin/movie">Back to Movie List</a>
</main>
</body>
</html>
